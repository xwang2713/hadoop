name: hadoop-hdfs
summary: Software platform for processing vast amounts of data
description: |
  Hadoop Distributed File System (HDFS™) is the primary storage system
  used by Hadoop applications.
  .
  HDFS creates multiple replicas of data blocks and distributes them on
  compute nodes throughout a cluster to enable reliable, extremely
  rapid computations.
provides:
  namenode:
    interface: dfs
requires:
  datanode:
    interface: dfs
  secondarynamenode:
    interface: dfs
